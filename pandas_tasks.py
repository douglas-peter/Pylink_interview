
if __name__ == "__main__":

    # 1) read csv, hint dates in CSVs are read as strings, please convert to datetime N.B. dates are stored in UK format
    # i.e. %d/%m/%Y

    # 2) Create a new dataframe from the original that shows the total sales in a month for each given customer, fill
    # NAs with zeroes and print the new dataframe

    # 3) Print the series containing the customer information from the original df where the maximum single sale occurs

